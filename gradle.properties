# Do not pin org.gradle.java.home to one PC.
# Gradle uses JAVA_HOME or the system Java 21 found on the current machine.
# If needed, set JAVA_HOME outside the project instead of committing a local path.
org.gradle.configuration-cache=false
